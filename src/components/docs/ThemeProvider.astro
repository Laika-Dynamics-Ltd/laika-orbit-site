---
// The site is dark only, so the docs are too: no light theme, no flash, no picker.
---

<script is:inline>
  document.documentElement.dataset.theme = 'dark'
  window.StarlightThemeProvider = { updatePickers() {} }
</script>
